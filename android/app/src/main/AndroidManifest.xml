<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ 카메라/마이크 권한: manifest의 최상위(=application 바깥)에 선언 -->
    <uses-permission android:name="android.permission.CAMERA" />            <!-- 카메라 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />      <!-- 동영상 녹화 시 오디오 -->

    <!-- (선택) 기기에 카메라 하드웨어가 있음을 선언.
         카메라 없는 기기도 설치되게 하려면 required="false" 로 둔다 -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />  <!-- ✅ -->

    <!-- (선택/상황별) 저장소 접근:
         - file_picker가 SAF를 쓰면 보통 불필요
         - 갤러리에 저장/읽기가 꼭 필요하고 API 28 이하 타겟이면 아래 두 줄 고려
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    -->
    <!-- (선택/상황별) Android 13+에서 기기 갤러리의 동영상 직접 접근 시 필요
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    -->

    <application
        android:label="body_log"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Flutter 텍스트 처리 플러그인이 쓰는 queries (원본 그대로) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
